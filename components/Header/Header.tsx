import type { NextPage } from 'next';
import Head from 'next/head';

const Header: NextPage = () => {
	return (
		<>
			<header>
				<div>Logo</div>
				<div>Search</div>
				<div>Minha Conta</div>
				<div>Carrinho</div>
			</header>
		</>
	);
};

export default Header;
